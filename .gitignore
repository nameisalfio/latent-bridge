# --- Python Artifacts ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# --- Environments ---
.venv/
env/
venv/
lb-env/
ENV/

# --- Data & Models (Crucial) ---
# We ignore everything but keep the folder structure via .gitkeep
data/*
!data/.gitkeep
models/*
!models/.gitkeep

# --- Hydra & Experiment Outputs ---
# Hydra creates an 'outputs' folder by default. 
# We ignore the actual runs to keep the repo light.
outputs/
multirun/
checkpoint_dir/*

# --- Experiment Tracking (WandB / MLFlow) ---
# WandB creates a local 'wandb' folder for sync metadata.
wandb/
mlruns/
.wandb

# --- IDEs & OS ---
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# --- Docker ---
.docker/